{% extends 'base.html.twig' %}

{% block title %}Modifier le titre de l’image{% endblock %}

{% block body %}
    <h1>Modifier le titre de l’image</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}